name: test step

on:
    push:
        branches:
            - master
    workflow_dispatch: ~

jobs:
    test_job:
        runs-on: ${{ matrix.os }}

        steps:
            -
                name: Checkout
                uses: actions/checkout@master
            -
                name: Use humdrum docker image
                uses: docker://wolfgangdrescher/humdrum

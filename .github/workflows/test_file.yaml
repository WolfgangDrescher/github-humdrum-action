name: test step

on:
    push:
        branches:
            - master
    workflow_dispatch: ~

jobs:
    test_job:
        runs-on: ubuntu-latest

        steps:
            -
                name: Checkout
                uses: actions/checkout@master
            -
                name: Use humdrum docker image
                uses: docker://wolfgangdrescher/humdrum
            -
                name: Use humdrum docker container
                run: hint 01-beatus-vir.krn
